# shadcn Base UI for client components

We decided to initialize shadcn in Base UI mode for the client when adding Story Points progress, badges, selects, and related feature-touched controls. We chose this over a local-only progress component or Radix-mode shadcn because the feature needs reusable UI primitives while keeping the component base aligned with Base UI and avoiding an unrelated full-app UI rewrite.
