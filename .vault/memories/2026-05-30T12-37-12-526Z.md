---
type: memory
created: '2026-05-30T12:37:12.526Z'
project: kanai
source: opencode
session: ses_187201992ffeF5UdL9svvDL4aj
tags:
  - memory
  - opencode
---
# Memory - Frontend Verification Gotchas

## Insight
In Kanai's `client/`, `bun --bun run build` can successfully regenerate TanStack Router route types, while `bun --bun run test` currently fails before test collection with Vitest/Bun worker `ReferenceError` messages about `dispose` and `listeners`; `bun --bun run check` may also be blocked by an existing import-order issue in `src/api/client/current-user.ts`.

## Use When
When verifying frontend changes, especially new file routes, distinguish route/build failures from the current unrelated Vitest/Bun startup failure and existing Biome import-order check failure.

## Backlinks
- None yet
